#include <bits\stdc++.h>
#include <utility>
#include <string.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    auto p = make_pair(1, 2);
    cout << p.first << '\n';
    cout << p.second << '\n';
    return 0;
}